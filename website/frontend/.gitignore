# Node.js / npm / Yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Logs and diagnostic reports
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Build outputs (Vite)
dist/
.cache
.parcel-cache
.vite/

# Environment variables (secrets!)
.env
.env.local
.env.*.local
!.env.example  # Keep example files for others to copy

# TypeScript cache
*.tsbuildinfo

# Firebase-specific
.firebase/      # Cache and generated files from firebase-tools
firebase-debug.log
firebase-debug.*.log

# Optional: If using Firebase Hosting/Functions and want to keep config versioned
# (Most teams commit these, but ignore if project-specific secrets are inside)
# .firebaserc
# firebase.json

# IDE / Editor files
.idea/          # JetBrains (WebStorm, IntelliJ, etc.)
.vscode/
*.swp
*.swo
.DS_Store       # macOS
Thumbs.db       # Windows

# Misc
*.tgz
coverage/
.nyc_output
.eslintcache
.stylelintcache
.node_repl_history